<% @content_for_title =
    case params[:action] 
    when 'latest'
      "刚刚发生的帖子"
    when 'tag'
      "关于#{@current_tag}的帖子"
    end
%>
<%= render :partial => 'common/pagination' %>
<%= render(:partial => @articles) %>
<%= render :partial => 'common/pagination' %>